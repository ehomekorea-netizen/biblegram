# 📌 Biblegram 개인 스티커 메모 (MEMO)

> Biblegram 프로젝트와 관련된 유용한 명령어와 과거 히스토리 및 참고용 템플릿을 저장하고 관리하는 메모장입니다. 

---

## 💻 1. 개발 및 배포 치트 시트 (Cheat Sheet)

### 🚀 로컬 개발 서버 구동
로컬(내 컴퓨터)에서 임시로 서버를 띄워 개발 화면을 미리 보고 싶을 때 사용합니다.
* **접속 주소**: [http://localhost:5173/](http://localhost:5173/)
* **명령어**:
  ```powershell
  # 1. 터미널(PowerShell 또는 CMD)에서 프로젝트 폴더로 이동
  cd "C:\Users\IN\.gemini\antigravity\scratch\biblegram"

  # 2. 로컬 개발 서버 실행
  npm run dev
  ```

### 💾 깃허브(GitHub) 저장 및 배포
수정한 코드를 안전하게 깃허브에 커밋하고 푸시하여 원격 저장소에 올릴 때 사용합니다.
```powershell
# 1. 변경된 모든 파일 추가
git add -A

# 2. 수정 상자를 이쁘게 포장하고 메모 남기기 (메시지는 자유롭게 변경 가능)
git commit -m "feat: 말씀 카드 생성 및 스플래시 화면 개선 완료"

# 3. GitHub 저장소에 최종 업로드하기 (Vercel 자동 배포 트리거)
git push origin main
```

---

## ⏰ 2. [참고용] PWA 말씀 알림 시간 변경 요청 템플릿

이전에 진행했던 **오전 말씀 알림 시간 변경 요청 사항**을 기록해 둔 메모입니다. 나중에 유사한 시간대 변경 및 문구 수정이 필요할 때 가이드라인으로 활용할 수 있습니다.

### 📋 요구 사항 및 지침
현재 구축되어 작동 중인 Vercel Cron Job 알림 시간을 변경할 때 적용할 지침입니다. 다른 로직이나 코드는 건드리지 않고 지정된 파일만 정밀 수정하여 배포합니다.

#### 1. 대상 시간
* 매일 오전 8시 KST (UTC 기준 23:00)

#### 2. 변경이 필요한 위치와 내용
* **`vercel.json`**:
  - `crons` 부분의 `schedule` 스케줄을 `"0 23 * * *"`로 수정.
  - *이유: Vercel Cron은 UTC 기준이므로 한국 시간 08:00에서 9시간을 뺀 이전 날 23:00으로 설정합니다.*
* **`api/cron-push.js`**:
  - 파일 내의 메시지 묶음 변수 이름과 내용을 '오후/마감' 뉘앙스에서 '오전/아침/시작' 뉘앙스로 자연스럽게 단어들만 교체.
  - *(예: '고요한 밤' ➡️ '상쾌한 아침', '하루 마감' ➡️ '새 아침 말씀 시작')*

> ⚠️ **주의 사항**
> 위 지침 외에 다른 DB 연동, 로직 수정, 기능 개선 등은 일절 배제하고 오직 명시한 시간 및 문구 치환만 처리한 후 Vercel 실서버 배포까지 다이렉트로 진행합니다.
